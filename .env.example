# Copy to .env; generate independent random values. No production defaults.
APP_ENV=development
APP_URL=http://localhost:3000
WEB_PORT=3000
POSTGRES_PASSWORD=GENERATE_MIGRATION_PASSWORD
RUNTIME_DB_PASSWORD=GENERATE_RUNTIME_PASSWORD
REDIS_PASSWORD=GENERATE_REDIS_PASSWORD
SESSION_SECRET=GENERATE_RANDOM_SECRET_AT_LEAST_32_CHARACTERS
# Seed runs only when explicitly requested.
DEV_SEED_PASSWORD=GENERATE_DEV_PASSWORD_AT_LEAST_12_CHARACTERS
